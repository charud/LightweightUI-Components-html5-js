/*var LC = LC || {};

LC.Data = (function(me)
{
    var _data = {};

    me.get = function(key)
    {
        return _data[key]; 
    };

    me.set = function(element, key, object)
    {
        _data[element] = object;
    };

    return me;
})(LC.Data || {});*/